import { assert } from 'chai';


suite('Confiugration Manager Test Suite', () => {
    test('should find configuration file in workspace if available', () => {
        assert(false);
    });

    test('should load default configuration if congif file is not available', () => {
        assert(false);
    });

    test('should create config object describing the current configuration', () => {
        assert(false);
    });

    test('should update all ESPCommands from the config object', () => {
        assert(false);
    });

    test('should update ESPToolPy properties from the connfig object', () => {
        assert(false);
    });
});
